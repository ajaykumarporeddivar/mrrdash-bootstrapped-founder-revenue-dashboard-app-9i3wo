/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export', // Uncomment this if you want to export as a static site
  // distDir: 'out', // Directory for static export
};

module.exports = nextConfig;